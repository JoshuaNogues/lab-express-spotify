<div>
    <div>
    {{#each track}}
    <div>
        <h3>{{name}}</h3>
        {{#if preview_url}}
        <p>Audio Preview</p>
        <audio src='{{preview_url}}' controls></audio>
        </div>
        {{/if}}
    {{/each}}
    </div>
</div>